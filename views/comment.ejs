<hr>
<p class="my-5 font-weight-bolder">Add comment</p>
<form action="/comments/<%= post._id %>" method="POST">
    <div class="form-group row">
        <label for="name" class="col-sm-2 col-form-label">Name</label>
        <div class="col-sm-8 offset-sm-2">
        <% if(user) { %>
        <p><%= user.fullname %></p>
        <% } else { %>
        <input type="text" id="name" placeholder="your name" >
        <% } %>
    </div>
    </div>
    <!-- <div class="form-group row">
        <label for="email" class="col-sm-2 col-form-label">Email</label>
        <div class="col-sm-8 offset-sm-2">
        <input type="email" id="email" placeholder="Email">
        </div>
    </div> -->
    <div class="form-group row">
        <label for="inputPassword3" class="col-sm-2 col-form-label">Comment</label>
        <div class="col-sm-8 offset-sm-2">
            <textarea id="body" name="body"></textarea>
        </div>
    </div>    
    <div class="form-group row">
        <div class="col-sm-10">
            <% if(user) { %>
                <button type="submit" class="btn btn-primary">Add comment</button>
            <% } else { %>
                <button type="submit" class="btn btn-primary" disabled style="cursor: not-allowed">Add comment</button>
            <% } %>
        </div>
    </div>
</form>